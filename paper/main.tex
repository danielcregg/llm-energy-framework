\documentclass[conference]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{hyperref}
\usepackage{siunitx}

\graphicspath{{figures/}}

\begin{document}

\title{Energy Consumption of Large Language Model Inference:\\A Multi-Architecture Benchmarking Study on GPU Hardware}

\author{\IEEEauthorblockN{Daniel Cregg}
\IEEEauthorblockA{South East Technological University\\
Waterford, Ireland\\
daniel.cregg@setu.ie}}

\maketitle

\begin{abstract}
We present a rigorous framework for measuring the energy consumption of large language model (LLM) inference on GPU hardware and apply it to benchmark 10 open-weight models spanning 1B to 14B parameters across five architectural families (Llama, Mistral, Qwen, Phi, Gemma). Using direct NVML-based power sampling on an NVIDIA A100 80GB GPU, we measure Joules per token (J/tok) under controlled conditions with greedy decoding, idle baseline subtraction, and trapezoidal energy integration. Our results reveal a sub-linear scaling law ($E \propto N^{0.93}$, $R^2=0.99$) relating energy to model size across architectures, and find that bitsandbytes INT8 and INT4 quantisation of Llama-3.1-8B \emph{reduces} per-token energy by 10\% and 20\% respectively---contrary to prior findings of 2.9--3.7$\times$ energy penalties. We additionally ground our measurements in Irish electricity grid carbon intensity data, demonstrating a 2.9$\times$ variation in per-token carbon emissions depending on time of day. All code, data, and reproducibility artifacts are publicly available.
\end{abstract}

\begin{IEEEkeywords}
energy efficiency, large language models, GPU benchmarking, inference energy, carbon emissions, sustainable AI
\end{IEEEkeywords}

\section{Introduction}

The rapid deployment of large language models (LLMs) has raised urgent questions about their energy footprint. While training costs have received significant attention~\cite{strubell2019energy, patterson2021carbon}, inference increasingly dominates total lifecycle energy as models are deployed at scale~\cite{desislavov2023trends}. Understanding and optimising inference energy is therefore critical for sustainable AI deployment.

Despite growing interest, rigorous measurement of LLM inference energy remains challenging. Software-based estimators (e.g., CodeCarbon) rely on simplified models that may not accurately reflect GPU power dynamics~\cite{jay2023experimental}. Hardware-level measurement via NVIDIA's Management Library (NVML) provides direct power readings but requires careful instrumentation, including SLURM GPU mapping, idle baseline subtraction, and appropriate integration methods.

This paper makes the following contributions:
\begin{enumerate}
    \item A validated, open-source framework for measuring LLM inference energy using direct NVML power sampling with trapezoidal integration and idle baseline subtraction.
    \item A comprehensive benchmark of 10 open-weight models (1B--14B parameters) across five architectural families on an A100 80GB GPU.
    \item Empirical scaling laws relating energy to model size across architectures, extending prior single-family studies.
    \item A reassessment of the bitsandbytes quantisation energy paradox on Llama-3.1-8B, finding that updated software reverses the energy penalty.
    \item Carbon impact analysis grounded in real Irish electricity grid data, demonstrating temporal variation in per-token emissions.
\end{enumerate}

\section{Background and Related Work}

\subsection{LLM Energy Measurement}

Prior work on measuring LLM energy falls into three categories: software estimators~\cite{lacoste2019quantifying, lannelongue2021green}, hardware-level measurement~\cite{samsi2023words, argerich2024measuring}, and analytical models~\cite{desislavov2023trends}. Software estimators like CodeCarbon use thermal design power (TDP) scaling, which introduces systematic error~\cite{jay2023experimental}. Hardware-level approaches using NVML or power meters provide higher accuracy but require careful experimental design.

Samsi et al.~\cite{samsi2023words} measured inference energy for several LLMs using NVML on an A100 cluster, finding sub-linear scaling with model size. Argerich and Pati\~no-Mart\'inez~\cite{argerich2024measuring} extended this to include quantised models, observing that na\"ive quantisation can increase total energy despite reducing power draw.

\subsection{The Quantisation Energy Paradox}

On-the-fly dequantisation via bitsandbytes incurs a throughput penalty that can dominate the power reduction from lower-precision arithmetic. Prior work on the energy-bench framework found that INT8 quantisation of Mistral-7B via bitsandbytes increased J/tok by 2.88$\times$ despite a 37\% reduction in power draw, because throughput dropped 4.5$\times$. Pre-quantised kernels (AWQ, GPTQ via vLLM) did not exhibit this paradox.

\subsection{Carbon-Aware Computing}

The carbon impact of computation depends on both energy consumed and the carbon intensity of the electricity grid. Dodge et al.~\cite{dodge2022measuring} demonstrated that scheduling ML training to align with low-carbon grid periods can significantly reduce emissions. We extend this concept to inference, using real Irish grid data from EirGrid.

\section{Framework Design}

Our framework follows a four-layer architecture:

\begin{enumerate}
    \item \textbf{Hardware Instrumentation} (Layer 1): NVML-based power sampling in a background thread at 10\,Hz, with SLURM GPU index resolution and trapezoidal energy integration.
    \item \textbf{Inference Engine} (Layer 2): Standardised model loading (FP16, INT8, INT4 via bitsandbytes) and inference execution with \texttt{torch.cuda.synchronize()} for precise timing.
    \item \textbf{Metric Computation} (Layer 3): Computes J/tok, tok/s, mean/peak watts, and optional gCO$_2$eq/tok from energy and inference measurements.
    \item \textbf{Orchestration} (Layer 4): Runs the benchmark protocol (3 warmup + 10 measured runs per configuration) and produces structured JSON reports with full metadata.
\end{enumerate}

Key design decisions include: (1) NVML-only power measurement (no software estimators), (2) idle baseline subtraction for net inference energy, (3) greedy decoding only for deterministic output, (4) 10 measured runs with 95\% confidence intervals, and (5) a fixed 5-prompt set spanning summarisation, QA, code generation, long-form explanation, and reasoning tasks.

\section{Implementation}

\subsection{Hardware}

All experiments were conducted on an NVIDIA A100 80GB PCIe GPU accessed via SLURM on an HPC cluster. The A100 has a TDP of 300\,W and an observed idle power of 58.9\,W ($\sigma = 0.3$\,W), which was remarkably stable across all experimental sessions. CUDA driver version 530.30.02 with CUDA 12.1 was used throughout.

\subsection{Software}

The framework is implemented in Python 3.11 using PyTorch 2.1.0+cu128, Hugging Face Transformers, and pynvml for direct GPU power queries. Quantisation uses bitsandbytes 0.44 for INT8 (\texttt{load\_in\_8bit}) and INT4/NF4 (\texttt{load\_in\_4bit}) precision. All models use greedy decoding (\texttt{do\_sample=False}) with \texttt{max\_new\_tokens=200}.

\subsection{Experimental Protocol}

For each (model, precision, batch size) configuration, the framework executes:
\begin{enumerate}
    \item 30-second idle baseline measurement (extended to 60\,s if CV $>$ 10\%)
    \item 3 warmup runs (discarded)
    \item 10 measured runs with concurrent power sampling
\end{enumerate}

Each run records: generation wall time (via \texttt{torch.cuda.synchronize()}), power samples at 100\,ms intervals, total and net energy (via trapezoidal integration minus baseline), input/output token counts, and generated text.

\section{Validation}

Before the full benchmark campaign, we validated the framework on \texttt{Qwen2.5-1.5B-Instruct}. The validation confirmed:
\begin{itemize}
    \item J/tok = 0.60 (bs=1), 0.14 (bs=4), within expected range for a 1.5B model
    \item Idle baseline = 58.9\,W ($\sigma = 0.2$\,W), stable and consistent
    \item CV $< 2.5$\% across all configurations, well below the 15\% threshold
    \item tok/s = 47 (bs=1), 192 (bs=4), consistent with A100 capabilities
\end{itemize}

\section{Empirical Benchmark Study}

\subsection{Model Selection}

We benchmark 10 models spanning five architectural families: Llama (1B, 3B, 8B), Qwen (1.5B, 7B), Gemma (2B, 9B), Phi (3.8B, 14B), and Mistral (7B). Three additional large models (Qwen-32B, Mixtral-8x7B, Llama-70B) were planned but could not be benchmarked due to disk space constraints on the shared HPC filesystem (207\,GB total). The Llama-3.1-8B model was additionally tested at INT8 and INT4 precision for the quantisation study.

Table~\ref{tab:summary} presents the key results. Across all FP16 models, J/tok at batch size 1 ranges from 0.45\,J (Llama-3.2-1B) to 4.55\,J (Phi-3-medium-14B), with the best efficiency achieved at batch size 16.

\input{tables/summary_table}

\subsection{Scaling Law Results}

Figure~\ref{fig:scaling} shows J/tok as a function of model parameters for all FP16 models at batch size 1. Fitting a power law $E = \alpha N^{\beta}$ yields $\beta = 0.93$ ($R^2 = 0.99$), indicating near-linear scaling of energy with model size across architectures. This contrasts with prior work on the Pythia family alone ($\beta = 0.80$), suggesting that cross-architecture heterogeneity introduces additional variance in the energy--size relationship.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig1_scaling_law}
    \caption{Scaling law: J/tok vs model parameters (FP16, bs=1). Power law fit yields exponent $\beta = 0.93$ ($R^2 = 0.99$).}
    \label{fig:scaling}
\end{figure}

\subsection{Batch Size Effects}

Figure~\ref{fig:batch} shows J/tok as a function of batch size for all models. Increasing batch size from 1 to 16 reduces J/tok by 7--15$\times$, with larger models benefiting more. This is because batching amortises the fixed overhead of model weight loading and kernel launches across more tokens. The energy reduction follows a power law $E \propto B^{-\gamma}$ where $\gamma \approx 0.9$ for most models.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig3_batch_size}
    \caption{J/tok vs batch size for all FP16 models, showing 7--15$\times$ reduction from bs=1 to bs=16.}
    \label{fig:batch}
\end{figure}

\subsection{Quantisation Impact}

Figure~\ref{fig:quant} presents the quantisation study on Llama-3.1-8B. Contrary to prior energy-bench findings on Mistral-7B (INT8: 2.88$\times$, NF4: 3.72$\times$ energy penalty), we observe that bitsandbytes quantisation \emph{reduces} per-token energy:
\begin{itemize}
    \item INT8: 0.90$\times$ energy (10\% reduction), despite 4.2$\times$ throughput penalty
    \item INT4: 0.80$\times$ energy (20\% reduction), despite 1.7$\times$ throughput penalty
\end{itemize}

The mechanism differs from prior work: while throughput drops substantially (42.6 tok/s FP16 $\rightarrow$ 10.1 tok/s INT8), power draw also drops dramatically (160\,W $\rightarrow$ 82\,W), yielding a net energy reduction. This reversal likely reflects improvements in bitsandbytes kernels between versions, and demonstrates that the quantisation energy paradox is software-version-dependent rather than fundamental.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig4_quantisation}
    \caption{Quantisation impact on Llama-3.1-8B: J/tok, tok/s, and watts across FP16, INT8, and INT4.}
    \label{fig:quant}
\end{figure}

\subsection{Architecture Comparison}

Figure~\ref{fig:arch} compares models of similar size across architectures. At the 7--9B scale, Qwen2.5-7B (0.16 J/tok at bs=16) and Mistral-7B (0.17 J/tok) are the most efficient, while Gemma-2-9b (0.23 J/tok) is least efficient despite having similar architecture depth. At the 1--3B scale, Llama-3.2-1B (0.03 J/tok at bs=16) is remarkably efficient.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig6_architecture}
    \caption{Architecture comparison: J/tok at bs=16 for models of similar size.}
    \label{fig:arch}
\end{figure}

\subsection{Carbon Impact}

Figure~\ref{fig:carbon} shows the gCO$_2$eq per 1000 tokens for each model computed using Irish grid carbon intensity data from EirGrid. Using a one-week sample (Jan 15--21, 2026) with intensity ranging from 119 to 348 gCO$_2$/kWh (mean 230), we find that per-token carbon emissions vary by 2.9$\times$ depending on time of day. For Phi-3-medium at bs=1, emissions range from 0.15 to 0.44 mgCO$_2$eq/token. This variation exceeds the factor-of-two difference between some models, suggesting that \emph{when} you run inference matters as much as \emph{which} model you choose.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{fig7_carbon_variation}
    \caption{Carbon emissions per 1000 tokens under min/mean/max Irish grid intensity (119--348 gCO$_2$/kWh).}
    \label{fig:carbon}
\end{figure}

\section{Discussion}

\subsection{Comparison with Prior Work}

Table~\ref{tab:crossstudy} compares our results with the energy-bench framework. The scaling exponent increases from 0.80 (single Pythia family) to 0.93 (multi-architecture), reflecting that different architectures have different energy characteristics at similar parameter counts. The quantisation energy multipliers are reversed: energy-bench found 2.88$\times$ (INT8) and 3.72$\times$ (NF4) penalties on Mistral-7B, while we find 0.90$\times$ and 0.80$\times$ reductions on Llama-3.1-8B. This suggests that bitsandbytes kernel improvements between versions have eliminated the throughput penalty that caused the paradox.

\input{tables/cross_study_comparison}

\subsection{Limitations}

Our study has several limitations: (1) single GPU type (A100 PCIe), (2) bitsandbytes-only quantisation (not AWQ/GPTQ), (3) greedy decoding only, (4) English-only prompts, (5) inference only (not training), (6) three planned large models (32B, 47B, 70B) could not be benchmarked due to disk constraints, and (7) the quantisation comparison uses different base models (Llama-3.1-8B vs energy-bench's Mistral-7B).

\subsection{Implications}

Our results have several practical implications for sustainable LLM deployment:
\begin{itemize}
    \item \textbf{Batch when possible}: Increasing batch size from 1 to 16 reduces per-token energy by 7--15$\times$.
    \item \textbf{Quantisation now helps}: With current bitsandbytes, INT4 quantisation reduces both memory and energy, making it doubly beneficial.
    \item \textbf{Right-size models}: A 1B model at bs=16 uses 14$\times$ less energy per token than a 14B model at bs=1 (0.03 vs 4.55 J/tok).
    \item \textbf{Schedule carbon-aware}: Running inference during low-carbon grid periods can reduce emissions by up to 2.9$\times$ without any model changes.
\end{itemize}

\section{Conclusion and Future Work}

We presented a rigorous NVML-based framework for measuring LLM inference energy and applied it to 10 models across five architectural families. Our key findings are: (1) energy scales as $N^{0.93}$ across architectures, (2) batching provides 7--15$\times$ efficiency gains, (3) bitsandbytes quantisation now \emph{reduces} per-token energy, reversing prior findings, and (4) Irish grid carbon variation introduces 2.9$\times$ emission variability.

Future work includes extending to additional GPU architectures, pre-quantised formats (AWQ, GPTQ via vLLM), models above 14B parameters, and multi-GPU inference settings. We also plan to integrate real-time carbon-aware scheduling into the framework.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
